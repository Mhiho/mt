import React from 'react';
// import { isLoggedIn, getUser, login } from '../src/services/auth';
import { Layout } from '../hoc/Layout';

const Login = () => {
  return (
    <Layout>
      <div>
        <h2>Zaloguj:</h2>
      </div>
    </Layout>
  );
};
export default Login;
